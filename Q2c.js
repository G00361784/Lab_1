const x = (y,z) =>{//taking two values to be added together
   console.log(y + z);//printing the result of two numbers to the console
}
x(2,3);//taking the two numbers i picked as arguments to the function